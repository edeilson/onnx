<!-- ABOUT THE PROJECT -->
## 📁 About The Project

This project runs inference on a set of ONNX models using a provided input image. It's designed for comparing performance across multiple image classification models exported in ONNX format. The models must be first chosen and download, after that the code classify image based on downloaded models.
